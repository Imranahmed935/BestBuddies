enum Gender {
  MALE
  FEMALE
}


enum Status {
  ACTIVE
  INACTIVE
  SUSPENDED
  DELETED
}

enum Role {
  USER
  ADMIN
}

enum PlanStatus {
  OPEN        
  FULL       
  COMPLETED   
  CANCELED
}

enum RequestStatus {
    PENDING
    APPROVED
    REJECTED  
    CANCELED
    ACCEPTED
}

enum ConnectionStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum NotificationType {
  FRIEND_REQUEST
  FRIEND_REQUEST_ACCEPTED
  FRIEND_REQUEST_REJECTED
  TRIP_JOIN_REQUEST
  TRIP_JOIN_ACCEPTED
  TRIP_JOIN_REJECTED
  MESSAGE
  SYSTEM
}

