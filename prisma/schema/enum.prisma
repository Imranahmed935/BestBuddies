enum Gender {
  MALE
  FEMALE
}


enum Status {
  ACTIVE
  INACTIVE
  SUSPENDED
  DELETED
}

enum Role {
  USER
  ADMIN
}

enum PlanStatus {
  OPEN        
  FULL       
  COMPLETED   
  CANCELED
}

enum RequestStatus {
    PENDING
    APPROVED
    REJECTED  
    CANCELED
}

enum ConnectionStatus {
  PENDING
  ACCEPTED
  REJECTED
}
